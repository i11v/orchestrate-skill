interface:
  display_name: "Orchestrate"
  short_description: "Orchestrate implementation and review"
  default_prompt: "Use $orchestrate to coordinate this implementation, steer the implementer and reviewer, and carry the task through reasonable fixes."
policy:
  allow_implicit_invocation: false
